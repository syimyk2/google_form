import { createSlice } from "@reduxjs/toolkit";
const initialState = {form: {}, answersData: {}, };
 export const FormSlice = createSlice({
  name: "form",
  initialState: {},
  reducers:{
   
  }
});

export const FormSliceActions = FormSlice.actions
